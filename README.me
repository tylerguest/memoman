# memoman

- Requirements: gcc (Linux)

Build object:
```sh
gcc -std=c99 -O2 -Isrc -c src/memoman.c -o memoman.o
```

Or compile directly:
```sh
gcc -std=c99 -O2 -Isrc your_app.c src/memoman.c -o app
```